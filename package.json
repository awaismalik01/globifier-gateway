{
  "scripts": {
    "start": "cd vercel && vercel dev --yes --local-config vercel.local.json"
  },
  "devDependencies": {
    "vercel": "^57.0.0"
  },
  "dependencies": {
    "@vercel/functions": "^3.7.6"
  }
}
